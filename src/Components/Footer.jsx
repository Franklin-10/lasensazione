import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={`${styles.footer} `}>
      <p>Desenvolvido por Franklin Oliveira</p>
    </footer>
  );
};

export default Footer;
